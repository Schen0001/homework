class Turtle {
    constructor(x, y) {
        this.x = x;
        this.y = y;
    }
}


const turtleStep1 = new Turtle(0, 0);
const turtleStep2 = new Turtle(2, 3);

// Sorry, I know how incomplete this is, took me too long to go over constructors 
// lesson, was trying to catch up on other classes before and after it. 
// Will turn it around for future assignments....
